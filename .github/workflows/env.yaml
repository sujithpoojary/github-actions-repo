name: using-env
on: push

env:
  GLOBAL: global

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GLOBAL: job
    steps:
      - run: echo "$GLOBAL-$GLOBAL-$GLOBAL"
        env:
          GLOBAL: step
